## Postman to Rested migration tool

Steps to run this tool.

Install all packages.

```
npm install
```

Run the tool:

```
node . postman_file.json [postman_environment_file]
```

The environment file is optional.
